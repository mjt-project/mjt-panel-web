import { state } from '../../app/state.js';
export function FilesView(){let rows=state.files.items.map(i=>`<tr><td>${i.type==='dir'?'📁':'📄'} ${i.name}</td><td>${i.size||'-'}</td><td>${i.modified||'-'}</td><td><button class="btn soft small" data-file-open="${i.name}" data-file-type="${i.type}">Open</button></td></tr>`).join('');return `<div class="files-layout"><div class="card"><div class="card-head"><div><h3>File Manager</h3><p class="muted">Profile: ${state.selectedProfile||'none'} • Path: ${state.files.path}</p></div><div><button class="btn soft small" id="fileUpBtn">Up</button> <button class="btn soft small" id="fileRefreshBtn">Refresh</button></div></div><table class="file-table"><thead><tr><th>Name</th><th>Size</th><th>Modified</th><th>Actions</th></tr></thead><tbody>${rows||'<tr><td colspan="4">No files or API unavailable.</td></tr>'}</tbody></table></div><div class="card"><div class="card-head"><div><h3>Editor</h3><p class="muted">${state.files.selected||'No file selected'}</p></div><button id="fileSaveBtn" class="btn primary small">Save</button></div><textarea id="fileEditor" placeholder="Select a text file to edit...">${state.files.content||''}</textarea></div></div>`}
