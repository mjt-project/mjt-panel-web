import { state } from '../../app/state.js';
import { escapeHtml, escapeAttr } from '../../utils/escape.js';

export function FileManagerView(entries = [], error = '') {
  return `<section class="grid gap-6 xl:grid-cols-[1.25fr_0.75fr]"><article class="rounded-3xl border border-mjt-border bg-white p-6 shadow-sm"><div class="mb-5 flex flex-col gap-3 lg:flex-row lg:items-start lg:justify-between"><div><h3 class="text-xl font-black">File Manager</h3><p class="text-sm text-slate-500">Profile: ${escapeHtml(state.selectedProfile || 'none')} • Path: ${escapeHtml(state.filePath || '/')}</p></div><div class="flex flex-wrap gap-2"><button class="rounded-xl border border-mjt-border px-3 py-2 text-xs font-black" data-file-action="up">Up</button><button class="rounded-xl border border-mjt-border px-3 py-2 text-xs font-black" data-file-action="refresh">Refresh</button><button class="rounded-xl bg-blue-600 px-3 py-2 text-xs font-black text-white" data-file-action="new-file">New file</button><button class="rounded-xl bg-cyan-500 px-3 py-2 text-xs font-black text-white" data-file-action="new-folder">New folder</button></div></div>${error ? `<div class="mb-4 rounded-2xl bg-red-50 p-3 text-sm font-bold text-red-700">${escapeHtml(error)}</div>` : ''}<div class="overflow-hidden rounded-3xl border border-mjt-border"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-3">Name</th><th class="px-4 py-3">Size</th><th class="px-4 py-3">Modified</th><th class="px-4 py-3">Actions</th></tr></thead><tbody>${entries.length ? entries.map(row).join('') : `<tr><td colspan="4" class="px-4 py-10 text-center text-slate-500">No files to show.</td></tr>`}</tbody></table></div></article><article class="rounded-3xl border border-mjt-border bg-white p-6 shadow-sm"><div class="mb-4 flex items-start justify-between gap-3"><div><h3 class="text-xl font-black">Editor</h3><p class="text-sm text-slate-500">${state.selectedFile ? escapeHtml(state.selectedFile) : 'No file selected'}</p></div><button id="saveFileBtn" class="rounded-2xl bg-blue-600 px-4 py-3 text-sm font-black text-white">Save</button></div><textarea id="fileEditor" class="codebox min-h-[520px] w-full rounded-3xl border border-mjt-border bg-slate-50 p-4 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-100" placeholder="Select a text file to edit...">${escapeHtml(state.fileContent || '')}</textarea></article></section>`;
}
function row(entry) { const isDir = entry.type === 'dir'; return `<tr class="border-t border-mjt-border hover:bg-slate-50"><td class="px-4 py-3 font-bold text-slate-800"><button class="text-left ${isDir ? 'text-blue-700' : ''}" data-file-open="${escapeAttr(entry.name)}" data-file-type="${escapeAttr(entry.type)}">${isDir ? '📁' : '📄'} ${escapeHtml(entry.name)}</button></td><td class="px-4 py-3 text-slate-500">${escapeHtml(entry.size || '-')}</td><td class="px-4 py-3 text-slate-500">${escapeHtml(entry.modified || '-')}</td><td class="px-4 py-3"><button class="rounded-xl border border-mjt-border px-3 py-1 text-xs font-bold text-slate-600" data-file-delete="${escapeAttr(entry.name)}">Delete</button></td></tr>`; }
