import { state } from "../../app/state.js";
import { escapeHtml, formatSize } from "../../utils/escape.js";
export function FileManagerView() { return `<section class="grid grid-cols-[minmax(0,1.5fr)_minmax(360px,.8fr)] gap-4"><article class="card card-pad"><div class="card-head"><div><h3 class="text-xl font-black">File Manager</h3><p class="text-sm text-slate-500">Profile: ${state.selectedProfile || "none"} · Path: ${escapeHtml(state.files.path)}</p></div><div class="flex flex-wrap gap-2"><button id="fileUpBtn" class="btn btn-soft btn-sm">Up</button><button id="fileRefreshBtn" class="btn btn-soft btn-sm">Refresh</button><button id="fileNewBtn" class="btn btn-soft btn-sm">New file</button><button id="folderNewBtn" class="btn btn-soft btn-sm">New folder</button></div></div><div class="table-wrap"><div class="file-row"><span>Name</span><span>Size</span><span>Modified</span><span>Actions</span></div>${state.files.items.map(fileRow).join("") || `<div class="empty">No files found.</div>`}</div></article><article class="card card-pad"><div class="card-head"><div><h3 class="text-xl font-black">Editor</h3><p class="text-sm text-slate-500">${state.files.selectedFile ? escapeHtml(state.files.selectedFile) : "No file selected"}</p></div><button id="fileSaveBtn" class="btn btn-primary btn-sm">Save</button></div><textarea id="fileEditor" class="textarea" placeholder="Select a text file to edit...">${escapeHtml(state.files.selectedContent || "")}</textarea></article></section>`; }
function fileRow(item) { const icon = item.type === "directory" ? "📁" : "📄"; return `<div class="file-row"><button class="text-left font-bold text-slate-800 hover:text-blue-700" data-file-open="${escapeHtml(item.name)}" data-file-type="${escapeHtml(item.type)}">${icon} ${escapeHtml(item.name)}</button><span>${formatSize(item.size)}</span><span class="text-slate-500 text-sm">${escapeHtml(item.modified || "")}</span><span class="flex flex-wrap gap-2"><button class="btn btn-soft btn-sm" data-file-open="${escapeHtml(item.name)}" data-file-type="${escapeHtml(item.type)}">Open</button><button class="btn btn-danger btn-sm" data-file-delete="${escapeHtml(item.name)}">Delete</button></span></div>`; }
