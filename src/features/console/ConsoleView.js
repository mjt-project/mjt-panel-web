import { state } from '../../app/state.js';
export function ConsoleView() {
  return `<section class="rounded-3xl border border-mjt-border bg-white p-6 shadow-sm"><div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h3 class="text-xl font-black">Console</h3><p class="text-sm text-slate-500">Selected profile: ${state.selectedProfile || 'none'}</p></div><div class="flex gap-2"><button id="pauseLogsBtn" class="rounded-xl border border-mjt-border px-3 py-2 text-xs font-black">${state.logsPaused ? 'Resume logs' : 'Pause logs'}</button><button id="clearConsoleBtn" class="rounded-xl border border-mjt-border px-3 py-2 text-xs font-black">Clear</button></div></div><pre id="consoleOutput" class="codebox min-h-[440px] max-h-[58vh] overflow-auto rounded-3xl bg-slate-950 p-4 text-sm text-green-100"></pre><div class="mt-4 grid gap-3 sm:grid-cols-[1fr_130px]"><input id="commandInput" class="mjt-input" placeholder="Type command, e.g. list" /><button id="sendCommandBtn" class="rounded-2xl bg-blue-600 px-4 py-3 font-black text-white">Send</button></div><div id="commandHistory" class="mt-3 flex flex-wrap gap-2">${state.commandHistory.map((cmd) => `<button class="rounded-full border border-mjt-border px-3 py-1 text-xs font-bold text-slate-600" data-history-command="${cmd}">${cmd}</button>`).join('')}</div></section>`;
}
