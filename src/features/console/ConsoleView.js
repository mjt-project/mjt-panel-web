import { state } from "../../app/state.js";
export function ConsoleView() { return `<section class="card card-pad"><div class="card-head"><div><h3 class="text-xl font-black">Console</h3><p class="text-sm text-slate-500">Selected profile: ${state.selectedProfile || "none"}</p></div><div class="flex gap-2"><button id="pauseLogsBtn" class="btn btn-soft btn-sm">${state.logsPaused ? "Resume logs" : "Pause logs"}</button><button id="clearConsoleBtn" class="btn btn-soft btn-sm">Clear</button></div></div><pre id="consoleOutput" class="console-box"></pre><div class="grid grid-cols-[1fr_120px] gap-3 mt-3"><input id="commandInput" class="input" placeholder="Type command, e.g. list" /><button id="sendCommandBtn" class="btn btn-primary">Send</button></div><div id="commandHistory" class="flex flex-wrap gap-2 mt-3">${state.commandHistory.map((c) => `<button class="btn btn-soft btn-sm" data-history-command="${c}">${c}</button>`).join("")}</div></section>`; }
