import { state } from '../../app/state.js';
import { escapeHtml } from '../../utils/escape.js';

export function ConsoleView() {
  return `<section><article class="min-h-[calc(100vh-145px)] rounded-3xl border border-mjt-line bg-mjt-card/80 p-5"><div class="mb-4 flex items-center justify-between gap-3"><div><h3 class="text-xl font-black">Console</h3><p class="text-sm text-mjt-muted">Selected profile: ${state.selectedProfile || 'none'}</p></div><div class="flex gap-2"><button id="pauseLogsBtn" class="rounded-xl border border-mjt-line px-3 py-2 text-xs font-black">${state.logsPaused ? 'Resume logs' : 'Pause logs'}</button><button id="clearConsoleBtn" class="rounded-xl border border-mjt-line px-3 py-2 text-xs font-black">Clear</button></div></div><pre id="consoleOutput" class="scrollbar-thin min-h-[420px] max-h-[58vh] overflow-auto rounded-2xl border border-mjt-line bg-slate-950 p-4 text-sm text-green-200"></pre><div class="mt-3 grid grid-cols-[1fr_120px] gap-3 max-sm:grid-cols-1"><input id="commandInput" class="rounded-2xl border border-mjt-line bg-slate-950 px-4 py-3 outline-none focus:border-cyan-400" placeholder="Type command, e.g. list"/><button id="sendCommandBtn" class="rounded-2xl bg-cyan-400 px-4 py-3 font-black text-slate-950">Send</button></div><div id="commandHistory" class="mt-3 flex flex-wrap gap-2">${state.commandHistory.map((cmd) => `<button class="rounded-full border border-mjt-line px-3 py-1 text-xs text-mjt-muted" data-history-command="${escapeHtml(cmd)}">${escapeHtml(cmd)}</button>`).join('')}</div></article></section>`;
}
