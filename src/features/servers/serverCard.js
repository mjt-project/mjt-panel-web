export function serverCard(p,sel=false,full=false){return `<div class="server-card ${sel?'selected':''}" data-select-profile="${p.name}"><div class="card-head"><div><h3>${p.name}</h3><div class="server-meta"><span>${p.type||'minecraft'}</span><span>port ${p.port||''}</span></div></div><span class="badge ${p.running?'running':'stopped'}">${p.running?'Running':'Stopped'}</span></div>${full?`<p class="muted">${p.workdir||''}</p>`:''}<div class="server-actions"><button class="btn success small" data-action="start" data-profile="${p.name}">Start</button><button class="btn warn small" data-action="stop" data-profile="${p.name}">Stop</button><button class="btn danger small" data-action="kill" data-profile="${p.name}">Kill</button><button class="btn soft small" data-action="console" data-profile="${p.name}">Console</button></div></div>`}
