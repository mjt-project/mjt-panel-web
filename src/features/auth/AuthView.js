import { state } from '../../app/state.js';
export function AuthView(){return `<section class="login-view"><div class="login-card"><div class="brand-row"><div class="logo">M</div><div><h1>MJT Panel</h1><p class="muted">Mini Minecraft control panel</p></div></div><div class="login-copy"><b>Welcome back.</b><br>Paste token, or use <code>dev</code> on localhost.</div><label>API Base</label><input id="apiBaseInput" value="${state.apiBase}"><label>Panel Token</label><input id="tokenInput" type="password" value="${state.token}" placeholder="Token or dev"><div class="login-actions"><button id="loginBtn" class="btn primary">Sign in</button><button id="demoBtn" class="btn soft">Demo</button></div><p id="loginMessage" class="form-message"></p><div class="login-help"><code>.mjt panel token reset</code><code>.mjt panel update</code></div></div></section>`}
export function setLoginMessage(t,d=false){const el=document.getElementById('loginMessage'); if(el){el.textContent=t||'';el.style.color=d?'var(--danger)':'var(--warn)';}}
